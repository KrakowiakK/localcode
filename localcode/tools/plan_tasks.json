{
  "name": "plan_tasks",
  "aliases": ["plan_tasks", "plan_task", "task_plan"],
  "category": "reasoning",
  "description": "Create/update/list a structured task plan. Use this to plan before execution when tasks should be run in separate branches.",
  "additionalProperties": false,
  "parameters": {
    "action": {
      "type": "string",
      "description": "Action: create | update | list"
    },
    "tasks": {
      "type": "array?",
      "items": {
        "type": "object",
        "properties": {
          "id": {"type": "string"},
          "description": {"type": "string"},
          "status": {"type": "string"},
          "priority": {"type": "string"}
        }
      },
      "description": "Task list for create"
    },
    "task_id": {
      "type": "string?",
      "description": "Task id for update"
    },
    "status": {
      "type": "string?",
      "description": "New status for update"
    },
    "summary": {
      "type": "string?",
      "description": "Summary for update"
    },
    "files_changed": {
      "type": "array?",
      "items": {"type": "string"},
      "description": "Files changed for update"
    },
    "error": {
      "type": "string?",
      "description": "Error message for failed task"
    }
  },
  "required": ["action"],
  "handler": "plan_tasks"
}
