{
  "name": "read",
  "aliases": [
    "read_file",
    "open_file"
  ],
  "category": "read",
  "description": "Read a file with line numbers. Use when text context is missing or stale. Do not repeat read on unchanged file unless new context is needed.",
  "feedback": {
    "read_is_directory": "FORMAT ERROR: {{tool:read}} failed: path is a directory.\nACTION: Use {{tool:ls}}(path) to list files, then call {{tool:read}} on a file path.",
    "read_file_not_found": "FORMAT ERROR: {{tool:read}} failed: file not found.\nACTION: Use {{tool:ls}}(path) or {{tool:glob}}(pat, path) to locate the correct file, then call {{tool:read}} with the valid path."
  },
  "additionalProperties": false,
  "parameters": {
    "path": "string",
    "offset": {
      "type": "number",
      "optional": true,
      "minimum": 0
    },
    "limit": {
      "type": "number",
      "optional": true,
      "minimum": 1
    },
    "line_start": {
      "type": "number",
      "optional": true,
      "minimum": 1
    },
    "line_end": {
      "type": "number",
      "optional": true,
      "minimum": 1
    },
    "diff": {
      "type": "boolean",
      "optional": true,
      "default": false
    }
  },
  "handler": "read"
}
