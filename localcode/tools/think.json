{
  "name": "think",
  "aliases": ["reasoning", "step_by_step"],
  "category": "reasoning",
  "description": "Think step by step about a problem. Use before implementing (stage=plan), after coding (stage=review), when debugging (stage=debug), or to summarize (stage=summary). Can be called multiple times.",
  "additionalProperties": true,
  "parameters": {
    "prompt": {
      "type": "string",
      "description": "What you want to think about"
    },
    "stage": {
      "type": "string",
      "description": "Optional thinking stage: plan, review, debug, or summary. Each stage focuses on different questions.",
      "enum": ["plan", "review", "debug", "summary"]
    }
  },
  "required": ["prompt"],
  "handler": "think",
  "model_call": {
    "mode": "self",
    "include_history": true,
    "temperature": 0.3,
    "max_tokens": 2000,
    "timeout": 120,
    "system_prompt_file": "prompts/think_default.txt",
    "stage_param": "stage",
    "stage_prompt_files": {
      "plan": "prompts/think_plan.txt",
      "review": "prompts/think_review.txt",
      "debug": "prompts/think_debug.txt",
      "summary": "prompts/think_summary.txt"
    }
  }
}
